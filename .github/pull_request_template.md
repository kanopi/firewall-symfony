## What changed

<!-- One or two sentences. What behaviour is different after this? -->

## Why

<!-- The problem, not the patch. If a design choice had a real alternative, say what it
     was and why it lost — that belongs in the code comment too. -->

## How it was verified

- [ ] `composer check` (PHPCS + PHPStan at max)
- [ ] `composer test:gate` (both suites, 100% line and method coverage)
- [ ] Exercised on the low end (PHP 8.1 / Symfony 6.4), or not applicable

## Notes for the reviewer

<!-- Anything surprising: a trap in kanopi/firewall this works around, a listener
     ordering constraint, a test that had to run in a subprocess. -->

- [x] Was AI used in this pull request?
