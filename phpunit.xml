<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         backupGlobals="false"
         failOnRisky="true"
         failOnWarning="true"
         displayDetailsOnTestsThatTriggerWarnings="true"
         displayDetailsOnTestsThatTriggerDeprecations="true"
         displayDetailsOnPhpunitDeprecations="true">
  <testsuites>
    <testsuite name="unit">
      <directory suffix="Test.php">tests/Unit</directory>
    </testsuite>
    <testsuite name="integration">
      <directory suffix="Test.php">tests/Integration</directory>
    </testsuite>
  </testsuites>
  <coverage>
    <report>
      <html outputDirectory="reports"/>
      <text outputFile="reports/coverage.txt" showUncoveredFiles="true" showOnlySummary="false"/>
      <xml outputDirectory="reports/xml"/>
    </report>
  </coverage>
  <logging>
    <!--
      In a directory of its own, holding nothing else. CircleCI's
      `store_test_results` takes a directory and parses every XML file under
      it as JUnit — which, pointed at `reports/`, meant the 43 files of
      PHPUnit's coverage XML as well, each rejected with "invalid top level
      element: phpunit". That failed the upload step in all thirteen test
      jobs while the tests themselves passed, so the run was green and the
      test results were never recorded.
    -->
    <junit outputFile="reports/junit/junit.xml"/>
  </logging>
  <source>
    <include>
      <directory suffix=".php">src/</directory>
    </include>
    <exclude>
      <!--
        Service definitions, not code: a closure the DI loader executes.
        Its behaviour is covered by the container tests, which compile it
        and assert on the resulting definitions, but PHPUnit cannot
        attribute those executed lines to a file that declares no class.
      -->
      <file>src/Resources/config/services.php</file>
    </exclude>
  </source>
  <php>
    <ini name="memory_limit" value="512M"/>

        <!-- ###+ symfony/framework-bundle ### -->
        <env name="APP_ENV" value="dev"/>
        <env name="APP_SECRET" value=""/>
        <env name="APP_SHARE_DIR" value="var/share"/>
        <!-- ###- symfony/framework-bundle ### -->
  </php>
</phpunit>
